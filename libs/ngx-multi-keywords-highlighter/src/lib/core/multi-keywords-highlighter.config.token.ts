import { InjectionToken } from '@angular/core';

/**
 * MULTI_KEYWORDS_HIGHLIGHTER_CONFIG_TOKEN
 */
export const MULTI_KEYWORDS_HIGHLIGHTER_CONFIG_TOKEN = new InjectionToken(
  'MULTI_KEYWORDS_HIGHLIGHTER_CONFIG_TOKEN',
);
